/**app.wxss**/
.container {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  padding: 200rpx 0;
  box-sizing: border-box;
} 

/* 全局：牌型出牌次数样式 */
.count-hash { color: #ddd; margin-right: 6rpx; }
.count-orange { color: #ff9800; font-weight: bold; }

/* 全局：金币显示样式 */
.gold-text { 
  color: #ffd700; 
  font-weight: bold; 
} 
